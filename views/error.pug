extends layout

block content
  article.grid-container.portfolio-intro
    h1= error.message
    h2= error.status
    img.round(src="/static/images/404.png", alt="Sorry not found")
    pre= error.stack